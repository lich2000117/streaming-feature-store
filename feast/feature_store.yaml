# Feast Feature Store Configuration
project: fraud_detection
provider: local
registry: data/registry.db

online_store:
  type: redis
  connection_string: "redis:6379"

offline_store:
  type: file

entity_key_serialization_version: 2

feature_server:
  enabled: true   # ❌ drop host/port here

flags:
  alpha_features: true
  beta_features: true
